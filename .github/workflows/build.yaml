name: build

on:
  push:
    branches:
      - main
      - 'renovate/**'

  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

env:
  DRY_RUN: ${{ github.ref != 'refs/heads/main' }}
  DOCKER_BUILDKIT: 1
  USER_NAME: ubuntu
  TEST_IMAGE: renovate/buildpack-test

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    timeout-minutes: 15

    strategy:
      matrix:
        tag:
          [
            dotnet,
            erlang,
            golang,
            java,
            helm,
            nix,
            node,
            php,
            powershell,
            python,
            ruby,
            rust,
            swift,
            latest,
          ]

    steps:
      - uses: actions/checkout@93ea575cb5d8a053eaa0ac8fa3b40d7e05a33cc8 # tag=v3.1.0

      - uses: sigstore/cosign-installer@ced07f21fb1da67979f539bbc6304c16c0677e76 # tag=v2.7.0

      - name: Init
        run: |
          echo "CACHE_WEEK=$(date +%U)" >> $GITHUB_ENV

      - name: Docker registry login
        if: github.ref == 'refs/heads/main'
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: build
        run: docker build -t ${{ env.TEST_IMAGE }} --build-arg USER_NAME .

      - name: test
        run: docker build --build-arg IMAGE=${{ env.TEST_IMAGE }} ./test/${{ matrix.tag }}

      - name: Build and Publish
        if: ${{ matrix.tag == 'latest' }}
        uses: renovatebot/internal-tools@ce39e29d526994d612e6dd0573764150581b809d # tag=v1.14.4
        with:
          command: docker-builder
          build-args: USER_NAME,CACHE_WEEK
          last-only: true
          major-minor: false
          dry-run: ${{ env.DRY_RUN }}
